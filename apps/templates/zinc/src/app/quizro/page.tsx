import type { Metadata, NextPage } from "next";

import { getSharedMetadata } from "#/lib/sharedMetadata";

// export const metadata: Metadata = getSharedMetadata({ title: "퀴즈로" });

const Page: NextPage = () => {
  return <div>Quizro - 개발예정</div>;
};

export default Page;
